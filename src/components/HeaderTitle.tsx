import React from 'react';
import { View, TouchableOpacity, Text, ImageBackground,
        StyleSheet, Image } from 'react-native';


const HeaderTitle = ({navigation, info}) => {

}


export default HeaderTitle;
